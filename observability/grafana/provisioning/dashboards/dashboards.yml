apiVersion: 1

# Dashboards are files in the repo, not clicked together in the UI, so they
# are reviewable and survive the container being destroyed.
providers:
  - name: job-tracker
    type: file
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
